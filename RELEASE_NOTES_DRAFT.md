# Release Notes Draft (continuous)

**Owner:** integration  
**Version recommendation:** Hold — do not invent major/minor; likely patch/minor after CRM park (Master approval required).  
**As of:** 2026-07-15 15:38 PT

## Candidate content (not released)

### Agent Communications
- Repository-backed `.agent-comms/` bus, CLI, locks, tests (tip `2c064b3`).

### Executive Command Center (pending merge)
- Option A exclusive docs/apps/flows/views/tests (`8c3f7d8`). Offline PASS.

### Client Portal & Data Rooms (pending merge)
- Portal package + security review + exclusive module indexes (`6998a7f`). Offline PASS. External access default Off.

### AI Governance & Work Queues (pending merge)
- HVCG_AI list schema extensions for gated queues (`fc1fa79`). Offline PASS.

### Finance Operations (pending merge)
- Exclusive finance docs, stubs, Power Apps screens (`cdb5f5b`). Offline PASS.

### Operations Hub (pending defect clear)
- Exclusive Ops flows/views/lists package (`a73929d`). Offline PASS; locked-index residue blocks naive merge.

### CRM
- Offline acceptance PASS; live E2E FAIL; canvas pending D-002; Prod untouched.

## Known issues
See QA_KNOWN_ISSUES.md / QA_DEFECT_LOG.md.
