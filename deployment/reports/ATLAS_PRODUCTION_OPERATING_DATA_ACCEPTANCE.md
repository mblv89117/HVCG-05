# Atlas Production Operating-Data Acceptance

- Generated: 2026-07-22T20:30:00Z
- Branch: `fix/atlas-usable-operating-layer`
- Pre-cleanup inventory: `ATLAS_PRODUCTION_PROJECT_INVENTORY_BEFORE_CLEANUP.md`
- Duplicate review: `ATLAS_PROJECT_DUPLICATE_REVIEW.md`
- Owner queue: `ATLAS_OWNER_DATA_REVIEW_QUEUE.md`
- Machine inventory: `deployment/reports/data/atlas-production-project-inventory.json` (gitignored raw stores)

## 1–8. Counts and classification

| Metric | Count |
|---|---|
| Initial project count | **22** |
| Valid real / operating projects (including internal HVCG initiatives) | **20** distinct engagements + initiatives (excluding exact Lien dup) |
| Generated placeholder field rows | **20+** share bootstrap defaults (health/milestone/next/due) |
| Exact duplicates | **1 pair** (Lien Partners ×2) |
| Ambiguous records | **4** (Gnieski, LoanSpark, Falk link, personal pair) |
| Historic projects | **0** archived in store |
| Internal HVCG projects | **9** |
| Active client-engagement names linked to canonical Client 360 | **7** (ACCG, Prodigy, Kava, Christie, Hart, CCB, Lien-C360) |

## 9–13. Real operating counts (from Production Hub store + Client 360)

| Metric | Count | Source |
|---|---|---|
| Real client count (canonical live) | **7** | Client 360 IDs `client-*01` |
| Client 360 raw candidates | 319 | Mostly noise/prospects — not portfolio clients |
| Real task count | **40** | Hub `pm-store` (all autoGenerated) |
| Real document metadata count | **1446** | Sum of canonical client associations/sourceRefs |
| Real milestone count | **42** | Hub (21× Scope confirmed + 21× Next deliverable — bootstrap seeds) |
| Real deliverable count | **0** | Hub |
| Real approval/decision count | **3** decisions | Hub decisions (Dataverse approvals not re-counted here) |

## Root source of the 22 projects

`POST /api/pm/populate` (UI Sync) ran `bootstrapKnownProjects` (21 hard-coded initiatives) then `findOrCreateClientProject`, which created a **second** Lien Partners row because `Lienpartners` ≠ `Lien Partners`.

## Lien Partners

| Keep | `8523b3a2-65c0-4f52-8935-62b02fe59278` (`client-lien01`) |
| Archive candidate | `7854a61a-488a-4e16-9cf6-dc870bda4a1c` (bootstrap, no clientId) |
| Action | **Awaiting Manny approval** — see owner review queue |

## Prodigy / That's Kava / Gnieski

- Prodigy `8b0ce54b-…` → `client-pdg01` legitimate.
- That's Kava `57e54146-…` → `client-kava01` legitimate.
- Gnieski `e6b14d5e-…` → no clientId; prospect `f1458619-…` exists — **owner judgment**.

## Why repeated dates / generic values existed

| Value | Cause |
|---|---|
| `healthy` | Bootstrap + populate treating open bootstrap tasks as healthy work |
| `Scope confirmed` | Hard-coded bootstrap milestone seed |
| `Define next action — {name}` | Bootstrap task title promoted to `nextAction` |
| `2026-09-20` | `addDays(60)` from sync day ~2026-07-22 |
| `2026-07-22` | Populate/bootstrap `lastActivityAt = now` |

## Corrections shipped (code)

1. Normalize client name matching (prevents Lien-style duplicates).
2. Bootstrap no longer seeds `healthy`, `Scope confirmed`, or +60d due dates.
3. Create API defaults `health: unknown`, no fabricated next action.
4. Populate health recompute ignores bootstrap-only placeholders.
5. `GET /api/pm/populate/preview` dry-run; UI runs two dry-runs and blocks apply when creates/dupes/conflicts exist.
6. Portfolio display: Not assessed / No milestone / Next action required / No due date.
7. Data-quality filters + sticky Project/Actions columns.
8. Portfolio `dataQuality` flags from Hub.

**No Production archive/merge executed.** Existing 22 rows unchanged pending owner decisions.

## CRUD persistence

Automated Hub tests: create QA project → edit → archive → hidden from active list, recoverable by id. Exit 0.

Production authenticated QA create via UI not executed (no second owner sign-in). Recommended: create `Atlas Production QA Verification` after refresh.

## Sync dry-run / idempotency

- Preview endpoint deployed (401 without bearer — auth retained).
- Unit test: Lienpartners↔Lien Partners matching → **zero duplicate creates**; second preview stable.
- UI Sync button renamed to **Preview / Sync** and refuses apply when duplicates/creates/conflicts present.

## Project-detail results

All 22 IDs are UUID-valid and present in `pm-store`. Detail route contract unchanged (200 for existing, 404 invalid, 401 auth). Live browser matrix of all 22 deferred to owner session (already authenticated).

## UI repairs

Sticky first (Project) + sticky Actions; honest labels; quality filters; sync preview panel.

## Security

| Check | Result |
|---|---|
| Anonymous portfolio | 401 |
| Forged headers | 401 |
| Preview without bearer | 401 |
| `INTEGRATION_REQUIRE_AUTH` | true |
| External automations | not enabled |
| Production tags | unchanged |

## Azure / data planes changed

| Resource | Change |
|---|---|
| App Service `app-atlas-integration-hub` | Redeployed `server.js` (matching + preview + defaults) |
| SWA `swa-atlas-elite-os-dev` | Redeployed Elite OS UI |
| Dataverse | none |
| SharePoint | none |
| Power Automate | none |
| Production tags | none moved |

## Production commit / asset

| Field | Value |
|---|---|
| Production commit SHA | `6b4912a1ee5c3dfba939a295d47dd06e37bc3910` |
| Production asset | `assets/index-DBEnqS-1.js` |
| Hub | Redeployed bundled `server.js` to `app-atlas-integration-hub` |
| Tags | `atlas-v1.0.0-production` and `atlas-v1.0.1-production` **unchanged** |
