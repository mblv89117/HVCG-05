# EOS store (Development)

Persisted Agent Bus 2.0 messages and offline bridge outbox drafts live here.

- `bus-messages.json` — Bus 2.0 persistence
- `bridge-outbox/` — additive agent-comms v1 drafts (live send disabled)
