# Atlas Hub production deploy evidence — 2026-09-09 17:49 UTC

Constitution: HVCG-CONSTITUTION-2026-09-04-v1.0  
Repository: mblv89117/HVCG-05  
Target branch: `production/atlas-core`  
Wrapper: `./scripts/local-hub-deploy.sh` → `./scripts/deploy-hub-guarded.sh`  
Elite: not redeployed.

Prior blocked attempt: `docs/evidence/hub-deploy-20260909-1119.md`.

## SHAs

| Field | Value |
|-------|--------|
| PREVIOUS_LIVE_SHA | `2d61fe65603b88d12d08456967c65dae8e5aec52` |
| CURRENT_CANDIDATE | `f6db86587b237bcf2c61e01919016763f8bc9c51` |
| POST_DEPLOY_LIVE_SHA | `f6db86587b237bcf2c61e01919016763f8bc9c51` |
| LIVE_ELITE_SHA | `b504e12245e57b016e2bab934ebb44e55747a7e8` |
| HISTORICAL_FLOOR | `2d61fe65603b88d12d08456967c65dae8e5aec52` |

CURRENT_CANDIDATE resolved at deploy time from `origin/production/atlas-core` HEAD after `git fetch`. POST_DEPLOY_LIVE_SHA equals CURRENT_CANDIDATE.

## Azure account (no credentials)

| Field | Value |
|-------|--------|
| AZURE_ACCOUNT_NAME | HVCG Production |
| Subscription ID | `ebc84d85-b5ff-4c4b-add1-b0a8de31b319` |
| Tenant ID | `3df46563-86f3-4414-87fd-84ba967741ef` |
| Signed-in user | `manny@highvaluecapitalgroup.com` |
| Account state | Enabled |

No tokens, secrets, publish profiles, or device codes recorded.

## Pre-deploy / guards

| Check | Result |
|-------|--------|
| ANCESTRY_CHECK | **PASS** — LIVE_PRODUCTION `2d61fe65…` is an ancestor of CURRENT_CANDIDATE `f6db8658…` |
| Ancestry guard | **PASS** — `candidate_includes_live_ancestry` |
| LEASE_RESULT | **PASS (flock)** — Azure blob lease on `sthvcgwebintake` unavailable; flock acquired `holder=cursor-12792` and released after verify |
| Hub build dependencies | **PASS** — `npm ci` + esbuild bundle 1.8mb |
| HMAC raw-secret header removal | **PASS** — live OPTIONS allow-headers: `content-type,x-atlas-module-key-id,x-atlas-module-timestamp,x-atlas-module-signature` |
| ClientCode fail-closed | **PASS** — present in candidate; live unauthenticated routes 401 with no client payload |
| GLOBAL_AUTO_RESPOND | **false** |
| Elite equalization | **not performed** — LIVE_ELITE unchanged |

## Deploy

| Field | Value |
|-------|--------|
| DEPLOY_RESULT | **PASS** |
| Command | `./scripts/local-hub-deploy.sh` |
| Zip | `deployment/artifacts/hub-f6db86587b237bcf2c61e01919016763f8bc9c51.zip` |
| Azure | `az webapp deploy` → `rg-atlas-prod` / `app-atlas-integration-hub` → `RuntimeSuccessful` |
| Wrapper verify | `/health.commit=f6db86587b237bcf2c61e01919016763f8bc9c51` |
| Exit | `DEPLOY_WRAPPER_EXIT:0` |

Guards were not bypassed. Elite was not redeployed.

## HEALTH_RESULT

`GET https://app-atlas-integration-hub.azurewebsites.net/health`

| Field | Value |
|-------|--------|
| HEALTH_RESULT | **PASS** |
| ok | true |
| commit | `f6db86587b237bcf2c61e01919016763f8bc9c51` |
| authRequired | true |
| insecureDevAuth | false |
| ba.configured / ba.reachable | true / true |
| pmBackend | sharepoint-graph, managed_identity, configComplete, listsConfigured |
| capitalBackend | sharepoint-graph, managed_identity, configComplete, listsConfigured |
| capital overlay | present, readable, not corrupt |
| websiteLeads.configured | true |
| clientDocumentPersistence | sharepoint list `HVCG_DocumentRequests`, configured |
| fabricSync honesty | delta |
| changeNotifications | ready |

## CLIENTCODE_SMOKE_RESULT

Unauthenticated probes (no Bearer token; no client records returned):

| Path | HTTP | Result |
|------|------|--------|
| `GET /api/pm/clients/SYN01` (entitled code) | 401 | `unauthorized` / Microsoft sign-in required |
| `GET /api/pm/clients/ZZZ99` (non-entitled / unknown) | 401 | `unauthorized` / Microsoft sign-in required |
| `GET /api/client360` | 401 | `unauthorized` |
| `GET /api/capital/command-center` | 401 | `unauthorized` |

CLIENTCODE_SMOKE_RESULT = **PASS** — entitled and non-entitled paths fail closed without exposing another client's data. Authenticated dual-path isolation was not run (no Hub Bearer token used).

## HMAC_RESULT

Module ingest is now routed on live Hub (was 404 on `2d61fe65`).

| Probe | Result |
|-------|--------|
| OPTIONS `/api/modules/ingest` (Elite origin) | 200; allow-headers = key-id + timestamp + signature only (**no raw secret header**) |
| POST unknown `x-atlas-module-key-id` + timestamp + signature, no raw secret | **503** `MODULE_INGEST_UNAVAILABLE` / Module ingest is not configured |
| POST missing key-id | **503** same fail-closed |
| POST raw secret headers only (`x-atlas-module-secret` / `x-shared-secret`) | **503** same fail-closed — raw secret does not authenticate |

HMAC_RESULT = **PASS (fail-closed, key ring not configured)** — invalid/unknown key-id cannot ingest. Valid signed happy-path was **not** exercised (no production module secret retrieved or sent; no business action; auto-response not enabled).

## Success standard

- STATUS = PASS
- POST_DEPLOY_LIVE_SHA = CURRENT_CANDIDATE
- ANCESTRY = PASS
- DEPLOY = PASS
- HEALTH = PASS
- GLOBAL_AUTO_RESPOND = false
- OWNER_ACTION_REQUIRED = none (login already completed)
