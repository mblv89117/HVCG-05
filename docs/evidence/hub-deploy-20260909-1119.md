# Atlas Hub production deploy evidence — 2026-09-09 11:19 UTC

**Superseded** by `docs/evidence/hub-deploy-20260909-1749.md` after Owner Azure login and successful forward-deploy.

Constitution: HVCG-CONSTITUTION-2026-09-04-v1.0  
Repository: mblv89117/HVCG-05  
Target branch: `production/atlas-core`  
Executor: Cursor cloud agent (not Owner workstation). Azure CLI installed in-session.  
Elite: not redeployed.

## SHAs

| Field | Value |
|-------|--------|
| PREVIOUS_LIVE_SHA | `2d61fe65603b88d12d08456967c65dae8e5aec52` |
| CURRENT_CANDIDATE | `f6db86587b237bcf2c61e01919016763f8bc9c51` |
| POST_DEPLOY_LIVE_SHA | `2d61fe65603b88d12d08456967c65dae8e5aec52` (unchanged; deploy not applied) |
| LIVE_ELITE_SHA | `b504e12245e57b016e2bab934ebb44e55747a7e8` |
| HISTORICAL_FLOOR | `2d61fe65603b88d12d08456967c65dae8e5aec52` |

CURRENT_CANDIDATE resolved by `git fetch origin production/atlas-core` then `git rev-parse origin/production/atlas-core`. Not a hard-coded historical tip.

## Pre-deploy validation

| Check | Result |
|-------|--------|
| ANCESTRY_CHECK | **PASS** — `git merge-base --is-ancestor 2d61fe65… f6db8658…` |
| Deploy guard (`scripts/deploy-guard-check.mjs`) | **PASS** — `candidate_includes_live_ancestry` |
| Deployment lease mechanism | **available** — `scripts/lib/deploy-lease.sh` (Azure blob + flock fallback) |
| Rollback path | **available** — `deployment/scripts/Rollback-HVCGCapitalHub.ps1` |
| Hub build dependencies | **available** — `npm ci` completed; esbuild bundled `apps/atlas-integration-api` (1.8mb) |
| HMAC raw-secret header removal | **present** in source and bundle: CORS allow-headers are `x-atlas-module-key-id`, `x-atlas-module-timestamp`, `x-atlas-module-signature` only. Comment: `Raw secret header intentionally omitted.` |
| ClientCode fail-closed | **present** — unknown ClientCode returns 403 `CLIENTCODE_UNMAPPED`; unit tests pass |
| GLOBAL_AUTO_RESPOND | **false** (source + unit test `hub-memory-honesty`) |
| Elite SHA equalization | **not requested / not performed** |

Unit tests run (22 pass / 0 fail):

- `apps/atlas-integration-api/tests/hub-module-ingest.test.ts`
- `apps/atlas-integration-api/tests/hub-memory-honesty.test.ts`
- `packages/atlas-identity-map/tests/registry.test.ts`
- `packages/atlas-integration-contracts/tests/envelope.test.ts`

## Azure authentication

| Field | Value |
|-------|--------|
| AZURE_ACCOUNT_NAME | *(not authenticated)* |
| Tenant expected | `3df46563-86f3-4414-87fd-84ba967741ef` |
| Subscription expected | `ebc84d85-b5ff-4c4b-add1-b0a8de31b319` (HVCG Production) |
| `az account show` | Failed: no CLI session |
| Azure MCP (`subscription_list`, `appservice_webapp_get`, `group_list`) | Timed out (no usable Owner session) |

`az login --tenant 3df46563-86f3-4414-87fd-84ba967741ef --use-device-code` was started and left running. Device code is **not** recorded here.

No credentials, tokens, or publish profiles were printed or stored.

## Deploy

| Field | Value |
|-------|--------|
| LEASE_RESULT | **NOT_ATTEMPTED** — blocked before `deploy-hub-guarded.sh` |
| DEPLOY_RESULT | **BLOCKED** — Owner Azure interactive login required. Guards were not bypassed. |

## Live verification (pre-deploy production)

`GET https://app-atlas-integration-hub.azurewebsites.net/health`

| Field | Value |
|-------|--------|
| HEALTH_RESULT | **ok=true** on previous SHA (not candidate) |
| commit | `2d61fe65603b88d12d08456967c65dae8e5aec52` |
| authRequired | true |
| insecureDevAuth | false |
| ba.configured / ba.reachable | true / true |
| pmBackend | sharepoint-graph, managed_identity, configComplete, listsConfigured |
| capitalBackend | sharepoint-graph, managed_identity, configComplete, listsConfigured |
| websiteLeads.configured | true |
| fabricSync honesty | delta; changeNotifications ready |

`/health.commit` does **not** equal CURRENT_CANDIDATE. Success standard not met.

## ClientCode isolation smoke

Unauthenticated probes only (no Bearer token available; no client data returned):

| Path | HTTP | Body (non-sensitive) |
|------|------|----------------------|
| `GET /api/pm/clients/SYN01` (entitled code) | 401 | `unauthorized` / Microsoft sign-in required |
| `GET /api/pm/clients/ZZZ99` (non-entitled / unknown) | 401 | `unauthorized` / Microsoft sign-in required |
| `GET /api/client360` | 401 | `unauthorized` |
| `GET /api/capital/command-center` | 401 | `unauthorized` |

CLIENTCODE_SMOKE_RESULT = **PASS (unauthenticated fail-closed)** — both entitled and non-entitled routes closed without leaking another client's data. Authenticated entitled-vs-denied isolation was **not** run (no Owner Hub token).

## HMAC live verification

Live Hub at `2d61fe65` does not yet expose module ingest:

| Probe | Result |
|-------|--------|
| `POST /api/modules/ingest` with key-id + timestamp + signature, **no** raw secret header, unknown key-id | **404** `{error:not_found}` |
| Same without key-id | **404** `{error:not_found}` |

HMAC_RESULT = **NOT_LIVE_ON_PREVIOUS_SHA** — expected until candidate is deployed. Source/unit/bundle verification of key-id / timestamp / signature / fail-closed unknown key-id = PASS. No production business action sent. Auto-response not enabled.

## Lineage

```
2d61fe65 (LIVE_PRODUCTION / HISTORICAL_FLOOR)
  → … Wave 0–3 (lease, ClientCode, HMAC secret removal) …
  → f6db8658 (CURRENT_CANDIDATE = origin/production/atlas-core HEAD)
```

LIVE_ELITE `b504e122…` is independent and was not equalized.

## Owner action

Complete Microsoft device-code login for the HVCG tenant on the running `az login` session (or a new one), then resume this agent so `./scripts/local-hub-deploy.sh` can acquire the lease and zip-deploy. Do not redeploy Elite. Do not set GLOBAL_AUTO_RESPOND.
