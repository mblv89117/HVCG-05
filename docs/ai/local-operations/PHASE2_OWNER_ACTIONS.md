# Owner Actions — Phase 2

1. Review Phase 2 docs under `docs/ai/local-operations/`.
2. Confirm selected model `glm-4.7-flash:q4_K_M` (or choose another installed model via `.secrets/local-ai.env`).
3. After review, may set `LOCAL_AI_ENABLED=true` **locally only**.
4. Keep writes/EVA/client emails Off.
5. Authorize Phase 3 explicitly before any live-client content or write paths.
