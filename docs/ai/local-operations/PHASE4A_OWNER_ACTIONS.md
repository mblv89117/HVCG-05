# Owner Actions — Phase 4A

1. Review benchmark at `deployment/reports/local-ai-phase4a/benchmark.json`
2. Confirm Fast model remains `qwen2.5:7b-instruct` in local `.secrets/local-ai.env`
3. Keep Writes / ExternalMessages / EVA / ClientEmails **Off**
4. Use side-by-side compare only locally for model-selection decisions
5. Explicitly authorize Phase 4B (OCR / SharePoint sync / writes) before any of those paths
