# Phase 4 Recommendation (do not start without authorization)

1. Owner-authorized installation of a distinct Fast Operations model
2. Optional OCR/document parsing for uploaded files (still manual upload, still redaction gate)
3. Deeper approval-queue UX mapped to SharePoint `HVCG_AIApprovals` (still no writes until separate authorization)
4. Batching duplicate administrative drafts
5. Still keep LocalAIWritesEnabled / EVA / client emails Off unless separately authorized
