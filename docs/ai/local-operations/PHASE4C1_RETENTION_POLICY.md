# Phase 4C-1 Retention Policy

Defaults (`DEFAULT_RETENTION`): staged originals 24h; temp page images 6h; extracted text 30d; model outputs 90d; audit 365d; archived reviews 180d; tombstones 730d.

Configurable via store retention settings. Production records never affected.
