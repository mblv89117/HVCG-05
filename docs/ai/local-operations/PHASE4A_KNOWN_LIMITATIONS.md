# Phase 4A Known Limitations

- Fast model still requires Manny approval on most drafts by policy
- Deep jobs remain slow (~60–90s on this hardware)
- Side-by-side compare runs two full generations (can be expensive)
- Model weights live only on local disk via Ollama — not in Git
- Edit Redactions remains API + limited UI; compare is local-only
