# Phase 3 Known Limitations

- Only one local model installed; Fast profile falls back to Deep (~1–2 min/job)
- Edit Redactions UI is API-complete; Elite UI exposes Approve/Cancel primarily
- Document/meeting/client enrichments are heuristic scaffolds plus model JSON — not OCR pipelines
- SharePoint schemas still not deployed
- Live owner-approved non-synthetic content requires explicit flag each pack
