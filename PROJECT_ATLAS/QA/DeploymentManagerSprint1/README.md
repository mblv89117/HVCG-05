# QA package — Deployment Manager Sprint 1

- `qa-results.json` — Playwright + navigation/permission/responsive checks
- `screenshots/` — desktop + mobile captures

Generate with `npm run qa` from the app directory.
